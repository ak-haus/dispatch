// Prime DISpatch — Next.js 15 PostCSS config (Tailwind v4).
//
// Tailwind v4 uses a single PostCSS plugin entry. Token canon is imported
// via globals.css through @prime-dispatch/tokens.

export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
