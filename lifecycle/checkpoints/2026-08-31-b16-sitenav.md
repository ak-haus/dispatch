---
title: build session close — 2026-08-31 B16 SiteNav fork retirement (second of three)
status: active
session_type: build
column: retire the microsite forks onto @prime-dispatch/ui, one fork per session
---

# Build session close — 2026-08-31 (B16 · SiteNav)

> **Landed on `claude/b16-sitenav`:** the fork's implementation ported into the
> library as the canon SiteNav — host-owned band, motion links, hosted
> theme-cycler — with its styling compiler-transplanted from utilities into a
> real `SiteNav.css` · the microsite switched to the subpath import · the fork
> AND its `useMagnetic` hook retired (the hook moved into the library — one
> copy, no fork) · all 14 pixel locks HELD: the port moved zero shipped
> pixels. No AK ruling was needed mid-session — the axe gate measured every
> ported ink pairing clean on dawn, and the dark-cycle wine carries the
> pre-existing OQ-6 filing forward unchanged.

## Open ritual — verified, not assumed

| Check | Result |
|---|---|
| `git log -1 origin/main` at start | `37f481b` (the B16-SearchPalette merge), fetched and in sync before reading anything |
| Working tree at session start | clean |
| Board read | B16 row + Build 20 opener + SearchPalette close record first-hand |
| Precondition re-verified | `@source "../../../../packages/ui/src"` stands at `global.css:21` with stories/tests subtracted — landed and proven at SearchPalette; nothing to land |
| Machine note | fresh Windows checkout: `pnpm install`, Playwright chromium, and the PP font rail (`fetch:fonts`) hydrated before any gate ran |

## The diff — every divergence classified

The twin was bucket D — canon-correct BEM with a real 219-line `SiteNav.css` —
but it was a different COMPONENT boundary wearing the name: a self-owned
three-slot band (wordmark slot + border-bottom hairline + its own CSS
hamburger whose `aria-controls` named an id that existed nowhere). The fork is
the nav CLUSTER inside the Masthead's band — the sticky, scroll-aware,
hide-on-down `<header>`, the wordmark and the mobile Dialog sheet are all
host-owned, which spec Field 1's "wordmark-adjacent positioning per layout"
permits and the shipped product IS.

**Ruled DELIBERATE (ported):** the host-owned-band composition · the three
variants as shipped (`reception` / `article` / `mobile`, where `mobile` is the
contents of the host's Dialog sheet — spec Field 2's compact-mobile realized
host-side, `onNavigate` closing the sheet) · currentPath-derived
`aria-current` (exact or trailing slash) · the motion register (magnetic links
at 0.32/lockY/8px; the `layoutId` indicator with the hover-preview state
machine — wine at rest on the active page, copper-deep while previewing) ·
the hosted ThemeCycler (spec Field 6): segmented three-way, `aria-pressed`,
RAW-string `localStorage['prime-cycle']` + `html[data-prime-cycle]` + the
`.dark` mirror on night · the A13 SearchTrigger contract
(`aria-haspopup="dialog"`, `aria-expanded`, conditional
`aria-controls="dispatch-search-palette"` — the id the library palette keeps
for exactly this seam) · `data-analytics` on every link · the landmark-tight
`<nav>` (affordances outside it) with explicit list roles.

**Ruled DRIFT (dropped):** the twin's band root + wordmark slot +
`MastheadWordmark` dependency · its CSS hamburger + 720px mobile menu (the
dangling-id latent axe failure dies with it) · `DEFAULT_LINKS` naming pages
that don't exist (Articles/Archive/About/Subscribe) · `isActive`/`isDisabled`
per-link props (superseded by currentPath derivation; disabled links
unexercised by any host) · the `wordmark`/`affordances`/`className`/rest
passthrough affordances (unexercised — same ruling as the palette's) · the
twin's 17px center-aligned band typography (the fork ships 13px/800
cluster links) · its `::after` static underline (superseded by the layoutId
indicator) · the fork's `group` marker class (no group-* consumer in its own
subtree — dead marker, dropped with the record). The twin's REBUILD
2026-05-10 tombstones (middot separators, upper-rule, small-caps SEARCH)
carry forward as anti-patterns in the new stylesheet header. The twin's
2026-05-10 BACKLOG note (right-align the link cluster) is satisfied by the
band's retirement — the cluster sits right of the wordmark via the host's
`justify-between`.

**Kept from the twin (the one exercised affordance):** the `links` prop shape
— now `{ label, href }[]` defaulting to the exported `navPages` (the shipped
content set), so the library stays content-injectable without changing what
ships.

## Port precision, recorded in the source headers

Hooks renamed to the `prime-site-nav__*` inventory. The styling transplant is
the B15 discipline applied to a utility-authored fork: every declaration in
the new `SiteNav.css` is the COMPILER'S OUTPUT for the fork's exact utility
set, extracted from the real pre-change production build — resolved
`--spacing`/.25rem and `--radius`/.625rem (the shadcn bridge var is
microsite-only, so `rounded-md` ships as `calc(.625rem - 2px)` and the story
lane renders it bridge-free) · rings resolved to the concrete two-layer
box-shadow (2px sky-low offset under 4px accent-prime) · the compiler's
`@media (hover: hover)` gates kept · opacity-modified colors carried with the
compiler's TWO-layer emission (full-opacity var() fallback + the color-mix
override inside its own `@supports (color: color-mix(in lab, red, red))`
gate), in **oklab** exactly as compiled — not the handwritten palette block's
oklch · `-webkit-backdrop-filter` authored by hand (the Lightning-pass
lesson, SearchPalette precedent) · the verbatim `transition-colors` property
lists. Defensive-only additions recorded as such (list/button resets,
`text-decoration: none` — computed-identical under the governed surfaces'
shared preflight). `useMagnetic` moved verbatim into the component directory
and exports through the SiteNav subpath (the `search.ts` precedent); the
Masthead's wordmark magnetic now imports it from there.

## The adversarial audit — three refuters, zero blocking

A 3-agent workflow (behavior parity · CSS fidelity · consumer contracts)
tried to refute the port. No blocking finding. Acted on: the article nav's
shared `__nav` hook silently inherited reception's `column-gap: 2rem` (the
fork's article nav had no gap utility — invisible with one flex child, held
exact anyway via a `--article` override) · the dropped `@supports` fallback
layer (restored, above) · a dead `import React` in the stories. Recorded,
not chased: `navPages` type widened from the literal tuple to
`readonly SiteNavLink[]` (deliberate library shape) · stories/tests are not
typecheck-gated by any committed tsconfig — systemic across all 18
story-bearing components, not a port regression — **filed to the board
ledger** · the lifecycle fixtures' imports resolve through no committed
infrastructure (pre-existing W3-S-A scope, all five fixtures).

## Verification battery — all green locally

| Rail | Result |
|---|---|
| typecheck (5 workspaces) | 0 errors |
| unit | ui **105/105** (100 + 5 new SiteNav contract tests) · microsite **81/81** · tokens **56/56** |
| story rail | **355/355**, a11y-as-error (SiteNav 8/8; 356→355: nine legacy band stories → eight ported cluster stories) |
| tokens-lint | clean (var()/color-mix over canon slots only; the gate reads comments too — learned when it flagged a header sentence) |
| e2e, pinned container | **39/39, twice** (pre- and post-audit-fix) — all 14 `-linux` pixel locks HELD; every axe floor green |
| built-CSS fidelity | 12 fork-only utilities dropped from the bundle; 28+ BEM rules in, declaration-equal to the compiled source; +~5.2 KB |
| rendered proof | real `pnpm build:astro` + preview at 1280px: cluster renders on shipped faces; trigger→palette open flips `aria-expanded`/`aria-controls` and Escape restores; cycler writes `data-prime-cycle` + the raw string; computed styles resolve the canon slots (wine `#6b2520` active + indicator, copper `#b87333` trigger, oklab washes, blur(8px)) |

The axe watch closed quietly: SiteNav's text sits on sky-low (copper-label
AA by token contract; active wine passes on dawn) and every window-warm
element is icon-only — no new ink pairing, no meta-ink need. The dark-cycle
active wine (dusk 3.14 · night 3.9 at 13px/800) is the standing OQ-6
filing, carried forward as the visible-not-gating a11y todo; its wordmark
half left this component with the band and lives in the MastheadWordmark
stories.

## What this session did not do

The defer test held. ChapterRail untouched (its own session, last). B17
untouched. F29 untouched. **Filed to the board ledger, not fixed:** the
library's `state/theme-cycler` zustand store persists JSON under the SAME
`prime-cycle` key the shipped raw-string protocol uses (the microsite's
blocking FOUC script reads the raw string; the library's
`themeCyclerBlockingScript()` parses zustand JSON) — two incompatible
protocols on one key, a latent collision if any surface ever mounts both;
converging them is its own adjudication. Also filed: the
stories/tests-not-typecheck-gated gap (systemic). The dispatch wiki's build
log stops at Build 10 (2026-08-19) — builds 11–19 uningested; noted for a
catch-up ingest, not chased here. No barrel import introduced; the live
graph consumes `custom/SiteNav` and `custom/SearchPalette` by subpath only.

## Next opener

**Build 21 — B16, ChapterRail** (759 LOC fork vs 169 twin, 4.5×): the last
fork, deliberately kept for the end — the largest divergence multiple of the
three. After it retires, B16 closes 3/3 and the CartographyCanvas
disposition (drop, or refile as a promotion) is the row's only remainder.
